# Stop words
STOP_WORDS = set(
    """
anu en taso a mu o lon sama tan tawa seme e la li pi
""".split()
)

# contractions = ["n't", "'d", "'ll", "'m", "'re", "'s", "'ve"]
# STOP_WORDS.update(contractions)

# for apostrophe in ["‘", "’"]:
#     for stopword in contractions:
#         STOP_WORDS.add(stopword.replace("'", apostrophe))
